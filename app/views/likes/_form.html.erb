<%= form_for @like, html: { class: "pure-form pure-form-aligned" } do |f| %>
  <% if @like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
      <% @like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <%= f.label :likeable_id %><br>
    <%= f.text_field :likeable_id %>
  </fieldset>
  <fieldset>
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </fieldset>
  <fieldset>
    <%= f.submit class: "pure-button pure-button-primary" %>
  </fieldset>
<% end %>
